using System;
using PierresBakery.Models;

namespace PierresBakery
{
  class Program
  {
    static void Main()
    {
      Console.WriteLine("*~*~*~*~*~*~*~*~*~*~*~*~*~*~*~*~*");
      Console.WriteLine("Welcome to the Pierres Bakery!");
      Console.WriteLine("How many breads would you like? A single loaf costs $5, and every third loaf is free. ");
      Console.WriteLine("Please enter a number:");
      // string stringNumber1 = Console.ReadLine();  
      Console.WriteLine("How many pasteries would you like? A single pastery costs $2, and every fourth pastery is free. ");
      Console.WriteLine("Please enter a number:");
      // string stringNumber1 = Console.ReadLine();
    }
  }
}
